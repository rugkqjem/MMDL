# 1차 실행 오답 분류와 1차→2차 정답 여부 변화 (2026-09-26)

- 대상: 1차 `outputs/qwen3vl4b_mmmu_val/` (max_new_tokens 16384), 2차 `outputs/qwen3vl4b_mmmu_val_32k/` (32768 / max_model_len 128000)
- 채점: 주 지표 `scores_qwen.csv` (`--parser qwen --extract final`). 1차 오답 341개, 1차→2차에서 정답 여부가 바뀐 문제 87개
- 재현: `python scripts/classify_errors.py --run1 outputs/qwen3vl4b_mmmu_val --run2 outputs/qwen3vl4b_mmmu_val_32k --output_dir outputs/analysis/error_taxonomy`
  - 규칙으로 정할 수 있는 건 규칙으로 정하고, 응답을 읽고 판단한 38개는 스크립트의 `MANUAL_*`에 사유 한 줄과 함께 적어 둠
- 결과 파일: `r1_wrong_classified.csv` (341행, 문제별 카테고리·근거), `flips_r1_r2.csv` (87행), `summary.json`
- 참고: 받은 zip에는 `outputs/analysis/open_failures/`가 없어서 기존 주관식 분석과 대조하지 못함. 주관식 판정은 이 분석에서 새로 함

---

## 1. 1차 오답 341개 분류

### 분류 기준 (위에서부터 순서대로 적용, 처음 걸리는 카테고리 하나에만 넣음)

| # | 카테고리 | 기준 | 판정 방법 |
|---|---|---|---|
| 1 | **응답 잘림** | `finish_reason == length` (16384 토큰에서 끊김). 최종 답을 말했는지와 상관없이 여기로 | 자동 |
| 1a | ─ 반복 루프 | 마지막 1만 자의 zlib 압축률 < 0.10 (BASELINE_NOTES 기준) **또는** 마지막 1만 자의 줄(15자 이상) 중 80% 이상이 응답 안에서 글자 그대로 반복된 줄 | 자동 |
| 1b | ─ 루프 없이 계산 중 잘림 | 위 두 지표 모두 해당 없음 + 읽어 보니 새 계산을 이어가는 중 | 자동 + 확인 |
| 2 | **최종 답 없음·추출 실패** | 채점기가 모델의 답을 받지 못했고, (a) 모델이 하나의 답을 고르지 않았거나 (b) 모델은 정답을 말했는데 추출 규칙이 엉뚱한 부분을 가져간 경우 | 읽고 판단 |
| 3 | **표기 차이** | 추출된 최종 답의 값은 정답과 같은데 형식이 달라 파서가 못 맞춘 경우. 분수↔소수, 근호, 천 단위 쉼표, 단위·단어 덧붙임, 대소문자, 동치 각도, 정답이 모델 값을 반올림한 값인 경우 | 읽고 판단 |
| 4 | **객관식 오답** | 끝까지 답했고 모델이 고른 보기가 정답과 다름 | 자동 (추출된 글자와 응답 끝부분 대조) |
| 5 | **주관식 오답** | 끝까지 답했고 값이 틀림 | 읽고 판단 |
| 6 | **애매** | 위 카테고리 사이에서 판단이 갈리는 경우. 억지로 넣지 않음 | 읽고 판단 |

- **추출 실패와 오답이 겹치면 오답으로 분류.** 추출이 제대로 됐어도 채점 결과가 오답이면 "오답의 원인"은 모델 답이기 때문. 이런 4개는 `flags=extract_fail`로 표시
- 모델 값이 정답보다 **덜 정밀한** 경우(24.3 vs 24.32)는 반올림인지 계산 실수인지 가를 수 없어 애매로 보냄. 반대로 모델 값이 더 정밀하고 정답이 그 반올림이면(1,249.24 vs 1249) 표기 차이

### 결과

| 카테고리 | 개수 | 비율 | 객관식 / 주관식 |
|---|---|---|---|
| 1a. 응답 잘림 — 반복 루프 | 72 | 21.1% | 70 / 2 |
| 1b. 응답 잘림 — 루프 없이 계산 중 | 3 | 0.9% | 1 / 2 |
| 2. 최종 답 없음 | 1 | 0.3% | 1 / 0 |
| 2. 추출 실패 (답은 맞음) | 1 | 0.3% | 0 / 1 |
| 3. 표기 차이 (값은 맞음) | 10 | 2.9% | 1 / 9 |
| 4. 객관식 오답 | 227 | 66.6% | 227 / 0 |
| 5. 주관식 오답 | 19 | 5.6% | 0 / 19 |
| 6. 애매 | 8 | 2.3% | 3 / 5 |
| **합계** | **341** | | 303 / 38 |

- 16384에서 잘린 오답은 78개 = 루프 72 + 계산 중 3 + 애매 3 (잘림 85개 중 나머지 7개는 루프 속 문장이 우연히 맞아 정답 처리)
- **모델 답은 맞았는데 채점에서 틀린 것**(표기 차이 10 + 추출 실패 1)은 11개 → 규칙을 고치거나 judge를 쓰면 최대 +1.2점. 애매 중 주관식 5개까지 더하면 최대 +1.8점
- **모델이 실제로 틀린 것**(객관식 227 + 주관식 19)은 246개로 오답의 72%

### 카테고리별 문제 목록 (적은 것만. 전체는 `r1_wrong_classified.csv`)

**1b. 루프 없이 계산 중 잘림 (3)**: Electronics_6 (메시 해석 진행 중), Electronics_11 (절점 방정식 진행 중), Energy_and_Power_15 (배관망 연립식 진행 중)

**2. 최종 답 없음 (1)**
- Agriculture_12: "no option is correct"라며 보기를 고르지 않음

**2. 추출 실패 — 답은 맞음 (1)**
- Electronics_25 (정답 50): `Answer:` 줄은 설명 문장이고 `50V`가 다음 줄에 있음 → 추출 규칙이 그 줄만 가져감

**3. 표기 차이 (10)**

| 문제 | 모델 답 | 정답 | 차이 |
|---|---|---|---|
| Basic_Medical_Science_10 | c | C | 주관식인데 정답이 글자 하나 → `MMMU_preproc`이 응답을 `A`로 바꾸고 정답은 `C`로 남아 **어떤 답을 내도 맞을 수 없음** (채점기 결함) |
| Biology_10 | `\dfrac{1}{64}` | 1/64 | LaTeX 분수 |
| Electronics_2 | `2\sqrt{2}` | 2.83 | 근호 (= 2.828) |
| Electronics_27 | 240 | -120 | 같은 각도 |
| Finance_17 | 1,000 | 1000 | 쉼표 |
| Finance_20 | 7,243,000 | 7243000 | 쉼표 |
| Finance_30 | 1,249.24 | 1249 | 쉼표 + 정답이 반올림값 |
| Manage_20 | 2,960 unfavorable | 2960 | 쉼표 + 단어 |
| Math_15 | `\frac{24}{7}` | ['24/7', '3.429'] | LaTeX 분수. 목록형 정답은 Qwen 채점기에서 문자열 그대로 비교돼 원래 맞을 수 없음 |
| Math_24 (객관식) | `r = \frac{2R}{3}` | A. r = 2R/3 | `Final Answer:` 줄은 LaTeX 수식, 뒤의 `Correct Option: **A**`는 추출 표시가 아님 |

**5. 주관식 오답 (19)**: Chemistry_4, 20, 23, 30 / Computer_Science_26 / Electronics_4, 5, 16, 21, 24, 26, 29, 30 / Finance_8, 10, 24 / Geography_14 / Pharmacy_26 / Physics_21

**6. 애매 (8)**

| 문제 | 무엇 사이에서 애매한가 | 내용 |
|---|---|---|
| Chemistry_13 | 표기 차이 ↔ 주관식 오답 | 13.0 vs 12.97. 유효숫자 3자리면 같지만 모델은 13.006으로 곱셈을 틀린 뒤 반올림 |
| Chemistry_25 | 표기 차이 ↔ 주관식 오답 | 24.3 vs 24.32. 모델 값이 덜 정밀 |
| Finance_23 | 표기 차이 ↔ 주관식 오답 | 527.74 vs 527.89 (0.03%). 방법은 같고 중간 반올림 차이로 보임 |
| Manage_3 | 표기 차이 ↔ 주관식 오답 | 242,159.73 vs 242,110.62 (0.02%) |
| Geography_4 | 주관식 오답 ↔ 부분 정답 | "Clearwater, Florida" vs ['Tampa', 'Florida']. 주(州)는 맞음. MMMU 파서라면 정답 처리 |
| Electronics_9 | 잘림: 루프 ↔ 계산 중 | 글자 그대로 반복은 아니지만 파형 가정을 바꿔 가며 같은 시도를 되풀이 |
| Math_12 | 잘림: 루프 ↔ 계산 중 | 제약식 해석을 바꿔 가며 매번 "objective 0"으로 끝남 |
| Mechanical_Engineering_4 | 잘림: 루프 ↔ 계산 중 | "Perhaps… Not matching" 순환. 일부 블록은 그대로 반복되지만 80% 기준 미만 |

### 객관식 오답 227개 참고
- 212개는 추출 결과가 보기 글자 하나. 추출된 글자와 응답 마지막 600자에서 모델이 마지막으로 말한 글자를 대조했고, 어긋난 5개는 직접 읽어 모두 모델의 최종 선택이 맞게 추출된 것을 확인
- 9개는 표시 없이 `D. Organic`처럼 한 줄로만 답함 (`flags=short_answer_no_marker`, 대부분 Agriculture)
- 4개는 추출도 실패했지만 모델이 고른 보기 자체가 틀림 (`flags=extract_fail`: Agriculture_23, Chemistry_26, Diagnostics_14, Manage_6)
- 응답 길이 중앙값 472토큰. 8192토큰을 넘긴 13개 중에는 "보기와 안 맞는다"를 반복하다 하나를 찍고 끝낸 경우가 있음 (예: Computer_Science_12 "I will go with C — because in many similar questions, the answer is C")

---

## 2. 1차 → 2차 정답 여부 변화 87개

같은 seed지만 응답이 완전히 같은 문제는 132/900뿐이라, 아래 변화는 대부분 **샘플링 경로가 달라진 결과**임.

### 분류 기준
- 두 실행 각각의 상태를 `finished` / `loop` / `trunc_calc`로 판정 (1번과 같은 루프 규칙. 2차 잘림 98개는 모두 `loop`)
- 둘 다 끝까지 답한 경우는 모델이 낸 최종 답 자체가 바뀌었는지, 답은 같은데 추출·표기만 달라졌는지 응답을 읽고 구분

### 결과

| 방향 | 바뀐 이유 | 개수 |
|---|---|---|
| **오답→정답 (43)** | 1차 잘림 → 2차 루프 없이 16384 안에 완주 | 12 |
| | 1차 잘림 → 2차 16384를 넘겨 완주 (예산 효과) | 1 |
| | 둘 다 완주, 최종 답이 달라짐 | 23 |
| | 둘 다 완주, 답은 같고 추출·표기가 달라짐 | 2 |
| | 2차는 루프인데 루프 속 문장이 우연히 정답 | 2 |
| | 둘 다 루프, 2차에서만 우연히 맞음 | 3 |
| **정답→오답 (44)** | 1차 완주(정답) → 2차 루프에 빠짐 | 20 |
| | 둘 다 완주, 최종 답이 달라짐 | 20 |
| | 둘 다 완주, 답은 같고 추출이 달라짐 | 1 |
| | 둘 다 루프, 1차에서만 우연히 맞음 | 3 |

**루프에 빠졌는지가 갈린 것: 35개** (12 + 1 + 20 + 2)
- 루프에서 벗어나 맞힌 13개 중 12개는 16384보다 짧게 끝남 (2차 토큰 3,397 ~ 14,199). 32768 예산 덕이 아니라 **이번엔 루프에 안 빠진 것**. 12개 중 11개는 1차에서 루프, 1개(Electronics_9)는 1차에서 루프 경계(애매)였음
- 예산 효과로 볼 수 있는 건 Architecture_and_Engineering_12 하나 (18,792토큰에 완주, 정답). 다만 1차에서는 16384 시점에 이미 반복 중이었으므로 같은 경로가 길어진 것은 아님
- 반대로 1차에서 끝까지 답해 맞힌 20개가 2차에서 루프에 빠짐. 1차 길이는 498 ~ 13,914토큰으로 짧은 것도 있어서(Biology_4 498, Music_5 1,031, History_12 1,145) 어려운 문제만 루프에 빠지는 것도 아님
- Materials_11, Mechanical_Engineering_1은 2차에서 루프에 빠졌는데 루프 속 "…1.49 kN, option D." 같은 문장이 추출돼 우연히 정답 처리됨

**둘 다 끝까지 답했는데 최종 답이 달라진 것: 43개** (23 + 20)
- 객관식 39개: 모델이 고른 글자 자체가 바뀜 (Sociology_10은 1차에 추출 표시 없이 답했지만 C → B로 답이 바뀐 경우)
- 주관식 4개: Chemistry_19 (4 → 2, 정답→오답), Finance_25 (0.286 → 0.952, 정답→오답), Finance_8 (860 → 360, 오답→정답), Physics_21 (D → A, 오답→정답)

**답은 같은데 추출·표기만 달라진 것: 3개** — 채점 규칙 쪽 변동
- Electronics_25 (오답→정답): 둘 다 50 V. 1차는 `Answer:` 뒤 설명 문장만 추출, 2차는 `\boxed{50 V}`
- Finance_20 (오답→정답): 둘 다 7,243,000. 1차는 쉼표 때문에 실패, 2차는 `\boxed{7243000}`
- Basic_Medical_Science_14 (정답→오답): 둘 다 "genetic engineering / recombinant DNA"로 답함(정답은 Transformation). 1차는 표시가 없어 응답 전체가 파서에 들어갔고 단계 목록 속 "Transformation"이라는 단어에 걸려 **정답으로 잘못 처리**된 것. 2차는 표시가 있어 정상적으로 오답

**둘 다 루프였는데 결과가 갈린 것: 6개** — Art_1, Art_Theory_3, Music_28 (2차만 우연히 맞음), Energy_and_Power_9, Energy_and_Power_17, Finance_5 (1차만 우연히 맞음). 모델이 답한 게 아니라 루프 속 문장이 우연히 맞은 것

### 1차 카테고리 기준으로 본 오답→정답 43개
객관식 오답 23, 반복 루프 15, 주관식 오답 2, 애매 1, 추출 실패 1, 표기 차이 1. 1차 반복 루프 72개 중 15개(21%)는 2차에 맞음 → 루프는 문제 자체보다 실행마다의 확률적 사건에 가까움

---

## 요약
- 1차 오답의 2/3는 모델이 끝까지 답했는데 보기를 잘못 고른 **객관식 오답**(227), 1/5은 **반복 루프로 잘린 것**(72)
- 채점 쪽 문제(표기 차이 + 추출 실패)는 11개(1.2점)뿐. 그중 Basic_Medical_Science_10은 정답이 글자 하나인 주관식이라 Qwen 채점 방식으로는 어떤 답도 맞을 수 없는 경우
- 1차→2차 변화 87개 = 루프 여부가 갈림 35 + 최종 답이 바뀜 43 + 추출·표기 차이 3 + 루프 속 우연 일치 6. **32768 예산이 직접 도운 건 1개**
