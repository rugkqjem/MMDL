# Qwen3-VL-4B MMMU-val 베이스라인 정리 (2026-09-23, 09-25·09-26 보완)

팀 회의 전 정리 노트. 오늘 `assignment1-vllm-eval` 브랜치 파이프라인으로 900문제를 처음 끝까지 돌린 결과와 결정 사항. 제출용 보고서(`reports/mmmu_baseline.md`, `docs/SUBMISSION_TEMPLATE.md` 형식)는 회의 후 이 내용을 바탕으로 작성.

## 한눈에

| | 점수 (macro, %) |
|---|---|
| 공식 (Qwen3-VL Technical Report) | 67.4 |
| **우리: Qwen 파서 + 최종 답 추출 (주 지표)** | **62.11** (559/900, Δ −5.29) |
| 우리: MMMU 파서 + 최종 답 추출 | 62.22 |
| 참고: 추출 없이 원본 파서에 응답 전체 입력 | Qwen 32.67 / MMMU 50.78 |
| 참고: 이전 `assignment1-eval` 브랜치 ("글자만 출력", 128 토큰, Text recipe) | 53.89 |

- 끝까지 생성된 815개만 보면 정확도 **67.73%**로 공식과 거의 같음. 격차의 대부분은 16384 토큰에서 잘린 85개(9.4%)에서 나옴.
- 잘림은 대부분 **반복 루프**라서 출력 예산을 32768로 늘려도 해결되지 않음 (09-25 재실행: 62.00점, 잘림 98개). 아래 "32768 재실행" 참고.
- **보고 점수는 09-23 첫 실행 한 번의 결과(62.11)와 그 응답만 사용.** 여러 실행의 평균은 쓰지 않음. 아래 두 재실행은 설정 검증·변동 확인용 참고 자료
- 결과 파일: `outputs/qwen3vl4b_mmmu_val/` (`predictions.jsonl` 원본 응답, `predictions_meta.json`, `scores_*`). 참고용 재실행: `outputs/qwen3vl4b_mmmu_val_32k/` (32768), `outputs/qwen3vl4b_mmmu_val_rerun/` (같은 설정 재실행)

## 실행 환경

| 항목 | 값 |
|---|---|
| 모델 / 데이터 | `Qwen/Qwen3-VL-4B-Instruct` @ebb281ec (bf16) / HF `MMMU/MMMU` @98e6ac0c, validation, 30과목 × 30 |
| 백엔드 | vLLM 0.30.0, transformers 5.17.0, torch 2.13.0+cu130, Python 3.12.14 (`requirements.lock.txt`) |
| GPU | A100-SXM4-80GB × 1 |
| 시간 | `infer.py` 전체 46.3분 (생성 42.1분). 채점은 CPU 수 초 |
| VRAM | nvidia-smi 최대 75.6GB. 대부분 vLLM이 미리 잡은 KV cache(59.9 GiB)이고, 가중치 등 9.0 GiB + activation 2.4 GiB |

```bash
bash scripts/run_mmmu_eval.sh --model_path Qwen/Qwen3-VL-4B-Instruct \
  --data_root <MMMU HF datasets cache> --output_dir outputs/qwen3vl4b_mmmu_val
  # 기본값: --max_new_tokens 16384 --max_model_len 32768
```

- venv는 **헤더(`Python.h`)가 있는 Python 3.12**가 필요. vLLM 엔진이 뜰 때 Triton이 C 확장을 컴파일함. 서버의 시스템 3.12에는 헤더가 없어 uv 관리 Python을 씀 (`uv venv --seed --python 3.12 .venv`)
- `load_dataset`은 validation만 써도 과목별 test split까지 받음 (첫 실행 때 약 3GB)
- 900개 요청을 `llm.generate()` 한 번에 넣음 (continuous batching). 응답 길이가 중앙값 450 ~ 최대 16384 토큰으로 편차가 커서 이 방식이 유리

## 파이프라인 설정

**프롬프트**: Qwen 공식 `evaluation/mmmu/run_mmmu.py` @96588727 `build_mmmu_prompt()`와 같음 (tech report Appendix B.1과도 같음). 시스템 프롬프트 없이 이미지를 모두 텍스트 앞에 둠.
```
<|im_start|>user
<|vision_start|><|image_pad|><|vision_end|>Question: {question}
Options:
A. {option_A}
B. {option_B}
...
Please select the correct answer from the options above.<|im_end|>
<|im_start|>assistant
```
주관식(53문제)은 `Question: {question}`만 들어감.

**샘플링**: 모델 카드 @ebb281ec "Generation Hyperparameters"의 **VL** recipe. Qwen 공식 MMMU 평가 코드(`run_mmmu.py`, `infer_instruct.sh`)도 같은 값
- `generation_config.json`에는 temperature / top_p / top_k / repetition_penalty만 있고 presence_penalty와 출력 길이는 없음
- temperature 0.7, top_p 0.8, top_k 20, repetition_penalty 1.0, presence_penalty 1.5
- seed 3407 (GitHub README 값. 공식 `run_mmmu.py`는 42)

**생성 예산**: `max_new_tokens` **16384**, `max_model_len` **32768** (입력 + 출력 합계)
- 2048 초과 240개(26.7%), 8192 초과 115개(12.8%), 16384에서 잘림 85개(9.4%). 2048로는 약 27%가 잘려서 제외
- 입력은 최대 약 5.6천 토큰(중앙값 약 1.1천)이라 `max_model_len` 32768이 16384 출력을 제한하는 일은 없음 (잘린 85개 모두 출력이 정확히 16384)
- 공식 자료끼리 값이 다름

  | 출처 | 출력 한도 | 입력+출력 한도 |
  |---|---|---|
  | 모델 카드 VL recipe | 16384 | — |
  | 모델 카드 Text recipe | 32768 | — |
  | Qwen 공식 `evaluation/mmmu/run_mmmu.py` 기본값, `infer_instruct.sh` @96588727 | 32768 | 128000 |

- 09-25에 공식 평가 코드 값(32768 / 128000)으로 다시 돌려 봤지만 점수가 같고(62.11 → 62.00) 순수 추론 시간은 3.4배(42분 → 2시간 24분). 16384를 넘겨 끝까지 답한 응답은 1/900뿐이라 **모델 카드 VL 값 16384로 확정**

**이미지**: `min_pixels = 1280·28·28`, `max_pixels = 5120·28·28` (공식 코드 값)
- 이 상수는 Qwen2.5-VL의 28px 기준. Qwen3-VL은 32px(patch 16 × merge 2)당 시각 토큰 1개라서 실제로는 이미지당 약 980~3,900 토큰
- 982장 모두 `min_pixels` 이상, 즉 작은 이미지는 약 1MP로 **확대**됨. 공식 파이프라인과 조건을 맞추려고 유지

## 채점 방식

`scripts/score.py`: **최종 답 추출 → 공개 파서 원본(수정 없음)**. 체크포인트만 바꿔 다시 돌릴 때도 같은 규칙이 적용되도록, 추출 규칙은 다른 벤치마크에서 쓰이는 공개 규칙만 사용.

1. **최종 답 추출** (`--extract final`, 기본값): 응답에서 가장 뒤에 나오는 표시를 찾음
   - 마지막 `\boxed{…}` — hendrycks/math `last_boxed_only_string`
   - 마지막 `answer is` / `Answer:` 뒤의 텍스트 — MMLU-Pro `extract_answer`/`extract_again`
   - 판단이 들어간 부분: 마크다운 `**`, `$` 제거와 `answer is:`(콜론)도 허용
   - 객관식은 추출 결과가 `C`, `C.`, `(C)`로 시작하면 그 글자만 넘김
   - **표시가 없으면 응답 전체를 넘김** (원본 방식과 같음)
2. **Qwen 파서** (주 지표): Qwen 공식 `eval_utils.py`의 `can_infer` (VLMEvalKit 계열). 주관식은 `{A: 정답, B: "Other Answers"}` 2지선다로 판정. 공식은 규칙이 실패하면 GPT-3.5 judge를 쓰지만 **우리는 judge 없이 실패 = 오답**
3. **MMMU 파서** (보조): MMMU 공식 `eval_utils.py`. 답을 못 찾으면 seed 42로 **무작위 선택**
- `--extract none`: 추출 없이 응답 전체를 파서에 넣는 원본 방식 → `scores_{parser}_raw.*`

**왜 추출이 필요했나**: 모델이 평균 2,846 토큰의 풀이를 쓰고 끝에 `Final Answer: **C. …**`처럼 답함. 풀이 전체를 넣으면 여러 보기 글자가 나와서 Qwen 파서는 504개 실패, MMMU 파서는 엉뚱한 글자를 고르는 경우가 많았음.

| | Qwen 파서 | MMMU 파서 |
|---|---|---|
| 추출 적용 | 62.11 (실패 96) | 62.22 (무작위 78) |
| 원본 방식 | 32.67 (실패 504) | 50.78 (무작위 63) |

**Qwen 파서를 주 지표로 정한 이유**
- 끝까지 생성됐지만 표시 없이 `D. Organic`처럼 짧게 답한 객관식 42개를 MMMU 파서는 37개 무작위 처리함. ` D `처럼 공백으로 둘러싸인 글자만 인식하고, 5단어 이하면 보기 내용 비교도 하지 않기 때문
- 같은 42개를 Qwen 파서는 40개 정상 처리함. fine-tuning으로 답이 짧아지면 MMMU 파서만 불리해질 수 있음

## 과목별 결과

| No. | Subject | Qwen | MMMU | 잘림 |
|---|---|---|---|---|
| 1 | Accounting | 73.33 | 80.00 | 5 |
| 2 | Agriculture | 53.33 | 43.33 | 0 |
| 3 | Architecture_and_Engineering | 46.67 | 63.33 | 12 |
| 4 | Art | 60.00 | 43.33 | 3 |
| 5 | Art_Theory | 86.67 | 80.00 | 1 |
| 6 | Basic_Medical_Science | 73.33 | 66.67 | 0 |
| 7 | Biology | 50.00 | 50.00 | 2 |
| 8 | Chemistry | 33.33 | 36.67 | 1 |
| 9 | Clinical_Medicine | 60.00 | 60.00 | 0 |
| 10 | Computer_Science | 60.00 | 60.00 | 0 |
| 11 | Design | 76.67 | 66.67 | 0 |
| 12 | Diagnostics_and_Laboratory_Medicine | 30.00 | 26.67 | 0 |
| 13 | Economics | 76.67 | 76.67 | 0 |
| 14 | Electronics | 46.67 | 43.33 | 5 |
| 15 | Energy_and_Power | 56.67 | 60.00 | 12 |
| 16 | Finance | 60.00 | 70.00 | 3 |
| 17 | Geography | 43.33 | 50.00 | 2 |
| 18 | History | 70.00 | 70.00 | 0 |
| 19 | Literature | 83.33 | 73.33 | 0 |
| 20 | Manage | 66.67 | 70.00 | 0 |
| 21 | Marketing | 83.33 | 83.33 | 0 |
| 22 | Materials | 53.33 | 60.00 | 11 |
| 23 | Math | 56.67 | 53.33 | 1 |
| 24 | Mechanical_Engineering | 43.33 | 56.67 | 13 |
| 25 | Music | 30.00 | 40.00 | 11 |
| 26 | Pharmacy | 76.67 | 76.67 | 2 |
| 27 | Physics | 76.67 | 76.67 | 0 |
| 28 | Psychology | 80.00 | 76.67 | 0 |
| 29 | Public_Health | 90.00 | 90.00 | 1 |
| 30 | Sociology | 66.67 | 63.33 | 0 |
| | **Overall (macro avg)** | **62.11** | **62.22** | **85** |

- Overall = 30개 과목 정확도의 단순 평균. 과목당 30문제라 micro(559/900)와 같음
- 잘림이 많은 과목(건축, 에너지, 재료, 기계, 음악)은 MMMU 점수가 더 높음. 답이 없는 잘린 응답을 MMMU 파서가 무작위로 골라 일부 맞힌 몫

## 32768 재실행 (09-25)

`--max_new_tokens 32768 --max_model_len 128000` (공식 평가 코드 값)으로 900문제를 다시 돌림. 나머지 설정은 동일.

| | 16384 (기본) | 32768 |
|---|---|---|
| Qwen 파서 + 추출 (주 지표) | 62.11 | 62.00 |
| MMMU 파서 + 추출 | 62.22 | 62.78 |
| 잘림 | 85 | 98 |
| 끝까지 생성된 응답의 정확도 | 67.73% (815개) | 68.20% (802개) |
| 순수 추론 시간 (`generate_seconds`) | 2,524.6초 (42분) | 8,666.7초 (2시간 24분) |

**예산을 늘린 효과는 거의 없음**
- 16384를 넘겨 끝까지 답한 응답은 900개 중 1개(18,792 토큰, 정답)
- 잘린 응답은 대부분 반복 루프 ("Perhaps…", "Wait —" 같은 문장을 끝없이 되풀이). 마지막 1만 자의 zlib 압축률로 판정하면 16384에서 잘린 85개 중 75개, 32768에서 잘린 98개 중 97개가 루프. 한 번 루프에 빠지면 예산을 얼마나 주든 한도까지 감
- 반복 없이 계산을 길게 이어가다 잘린 경우는 16384에서 4개뿐 (건축·전자·기계 계산 문제)

**같은 seed인데 응답이 대부분 다름 → 문제별 결과는 샘플링 노이즈**
- 900개 중 132개만 응답이 완전히 같음. 처음에는 `max_model_len` 변경으로 배치가 달라진 탓으로 추정했으나, **같은 설정 재실행(09-26)에서도 145개만 같아서 설정과 무관한 실행 간 변동으로 확인** (아래 절 참고)
- 문제별 정답 여부: 813/900(90.3%) 같음. 계속 정답 515, 계속 오답 298, 틀렸다가 맞음 43, 맞았다가 틀림 44. McNemar p = 1.00 (차이 없음)
- 바뀐 87개의 원인
  - 루프에 빠졌는지가 갈림 (약 33개): 16384에서 루프였다가 이번에 끝까지 답해 맞힌 13개, 지난번엔 맞혔는데 이번엔 루프에 빠진 20개. 이번에 끝까지 답한 응답도 대부분 16384보다 짧게 끝났으므로 예산 덕이 아니라 **이번엔 루프에 안 빠진 것**
  - 둘 다 끝까지 답했는데 최종 답이 달라짐 (46개): 풀이 경로가 달라진 샘플링 노이즈
- 루프에 빠질지는 실행마다 확률적으로 정해짐. 비율은 약 10%로 비슷하고, 47문제는 두 번 다 루프
- 잘린 응답이 정답 처리된 경우(16384에서 7개, 32768에서 11개)는 루프 도중 문장이 우연히 정답과 맞은 것으로, 모델이 답을 낸 게 아님
- 과목당 30문제라 과목별 점수는 한 번 실행에 몇 문제 차이로 10점 이상 흔들릴 수 있음 (에너지·전력: 맞았다가 틀림 7, 틀렸다가 맞음 1)

## 같은 설정 재실행 (09-26)

16384 결과와 32768 결과의 차이가 순수 샘플링 노이즈인지, max token 변경에 따른 배치 변화 때문인지 가리기 위해 **첫 실행과 완전히 같은 설정**(인자, seed 3407, 모델·데이터 revision, 같은 venv와 GPU)으로 900문제를 다시 돌림 → `outputs/qwen3vl4b_mmmu_val_rerun/`

| | 16384 첫 실행 (보고 점수) | 같은 설정 재실행 | 32768 |
|---|---|---|---|
| Qwen 파서 + 추출 | **62.11** | 63.56 | 62.00 |
| MMMU 파서 + 추출 | 62.22 | 62.67 | 62.78 |
| 잘림 | 85 | 78 | 98 |
| 끝까지 생성된 응답의 정확도 | 67.73% | 68.37% | 68.20% |
| 순수 추론 시간 | 2,524.6초 | 2,437.8초 | 8,666.7초 |

| 짝 비교 | 설정 | 응답이 완전히 같음 | 정답 여부 같음 | 틀림→맞음 / 맞음→틀림 | McNemar p |
|---|---|---|---|---|---|
| 첫 실행 vs 같은 설정 재실행 | 같음 | 145 / 900 | 807 | 53 / 40 | 0.21 |
| 첫 실행 vs 32768 | max token 다름 | 132 / 900 | 813 | 43 / 44 | 1.00 |
| 같은 설정 재실행 vs 32768 | max token 다름 | 142 / 900 | 800 | 43 / 57 | 0.19 |

- **설정이 같아도 다른 정도가 같음** → 32768과의 차이는 max token·배치 변경 때문이 아니라 **실행마다 생기는 변동**
- 응답은 생성 초반에 갈라짐: 앞 200자는 약 80%가 같지만 앞 1000자까지 같은 건 약 27%
- 추정 원인: vLLM이 요청을 끝나는 대로 빼고 채우며 배치를 동적으로 구성하므로 실행마다 배치 순서·수치가 미세하게 달라지고, 요청별 seed가 같아도 샘플링이 갈라짐 (vLLM은 배치와 무관한 동일 결과를 보장하지 않음)
- 같은 설정에서도 한 번 실행한 점수가 약 1.5점 차이로 나올 수 있음 (62.11 vs 63.56). 세 실행에서 결과가 바뀐 문제는 140개(15.6%), 세 번 다 잘린 문제 36개
- **fine-tuning 후에도 한 번 실행한 결과로 비교하므로**, 1.5점 안팎의 차이는 노이즈일 수 있다는 점을 해석할 때 감안. 같은 문제끼리 짝지은 비교(McNemar)로 차이가 우연인지 확인 가능

## 공식 수치(67.4)와의 격차: 근거

1. **잘림 (주원인)**
   - 85개(9.4%)가 16384 토큰에서 끊겨 최종 답이 없음 → 7개만 정답 (그마저 루프 도중 문장이 우연히 맞은 것)
   - 끝까지 생성된 815개의 정확도는 67.73%
   - 잘림이 5개 이상인 7과목의 macro 50.0, 나머지 23과목 65.8
   - 잘린 85개 중 75개는 반복 루프. **예산을 32768(공식 평가 코드 값)로 늘려도 해결되지 않음** (09-25 재실행: 잘림 98개, 점수 62.00)
   - 따라서 공식 파이프라인도 같은 설정이면 비슷한 루프가 생겼을 것. 남은 격차는 judge, 실행별 변동, 공식 쪽 실행 환경 차이 등에서 찾아야 함
   - 단, 잘린 문제는 원래 어려운 문제일 수 있어서 67.73은 상한 추정치
2. **judge 생략**
   - 끝까지 생성됐는데 Qwen 규칙이 실패한 38개 (다 맞았다고 쳐도 최대 +4.2점)
   - 그중 32개가 주관식이고, `\dfrac{1}{64}` vs `1/64`, `2\sqrt{2}` vs `2.83` 같은 표기 차이가 섞여 있음. 공식 GPT judge라면 일부는 정답 처리했을 것
3. **파싱 방식의 민감도**: 같은 응답인데 추출 유무로 32.67 ↔ 62.11. 풀이형 응답에서는 채점 방식이 점수를 좌우함
4. **샘플링 변동**: temperature 0.7이라 실행마다 결과가 달라짐. 표준오차 약 ±1.6점. 같은 설정으로 다시 돌리면 문제의 약 10%(93개)가 정답 여부가 바뀌고 점수는 63.56 (09-26 재실행). 보고 점수는 첫 실행 한 번의 결과
5. **데이터 소스** (09-25 확인): 공식은 VLMEvalKit `MMMU_DEV_VAL.tsv`, 우리는 HF pinned 데이터. validation 900문제를 한 장씩 비교함
   - **해상도는 같음**: 문제별 이미지 수와 982장의 가로·세로가 모두 일치 → 리사이즈 결과와 시각 토큰 수도 같음
   - 형식 차이: TSV는 JPEG(한 장짜리 문제는 공식 코드가 `.jpg`로 한 번 더 저장), HF는 PNG. 픽셀 차이는 이미지당 평균 절댓값 중앙값 1.28, 상위 10% 3.49 (0~255)로 작음
   - 투명 배경: 실제로 투명한 부분이 있는 17장 중 12장이 공식 TSV에서는 검은색, 우리는 흰색(`qwen_vl_utils.to_rgb`)으로 채워짐. 격차 요인으로는 작음
   - 서버의 TSV MD5는 VLMEvalKit 등록값(`585e8ad7…`)과 같고 Qwen 코드의 값(`521afc0f…`)과는 다름. Qwen 코드는 MD5가 다르면 다시 받아 그대로 쓰므로 공식도 이 파일을 씀

## 회의에서 정할 것
- **주 지표를 Qwen 파서 + 추출로 확정할지** (MMMU 파서는 보조로 같이 보고)
- ~~`max_new_tokens`~~: **16384 / `max_model_len` 32768로 확정** (09-25). 32768로 돌려도 점수가 같고 시간은 3.4배
- **judge**: 규칙 실패분에만 LLM judge를 쓸지. 재현성과 비용 문제가 있음
- ~~반복 실행~~: **한 번 실행한 결과와 그 응답으로만 보고·비교하기로 결정** (평균 사용 안 함). 실행 간 변동(같은 설정에서 약 1.5점, 문제의 약 10%)은 해석할 때 참고하고, 필요하면 문제별 짝 비교(McNemar)로 확인
- **잘린 응답 처리**: 루프로 잘린 응답은 무조건 오답으로 볼지 (현재는 추출·파서가 루프 도중 문장에서 우연히 정답을 찾기도 함. 오답 처리하면 16384 실행 62.11 → 약 61.33). 잘림 수는 점수와 별도로 보고
- 제출 보고서 분담, 팀명

## 확인 못 한 것
- 24GB GPU(RTX 4090)에서 실행: 가중치는 bf16으로 약 9GB라 `--gpu_memory_utilization`, `--max_model_len`을 낮추면 될 것으로 보지만 미검증
